<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
		xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
		xmlns:local="clr-namespace:Training.Forms"
		x:Class="Training.TaskListsPage"
		Title="Task Lists">
	<ContentPage.Resources>
		<ResourceDictionary>
            <local:IncompleteCountToStringConverter x:Key="IncompleteToString" />
		</ResourceDictionary>
	</ContentPage.Resources>
    <ContentPage.ToolbarItems>
		<ToolbarItem Text="+" Command="{Binding AddCommand}"/>
		<ToolbarItem x:Name="_logoutButton" Text="Logout" Priority="1" />
	</ContentPage.ToolbarItems>
	<ContentPage.Content>
        <ListView ItemsSource="{Binding TaskLists}" SelectedItem="{Binding SelectedItem, Mode=TwoWay}">
            <ListView.ItemTemplate>
				<DataTemplate>
                    <TextCell Text="{Binding Name}" Detail="{Binding IncompleteCount, Converter={StaticResource IncompleteToString}}" />
				</DataTemplate>
			</ListView.ItemTemplate>
		</ListView>
	</ContentPage.Content>
</ContentPage>
