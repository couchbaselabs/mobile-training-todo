<views:MvxWpfView xmlns:views="clr-namespace:MvvmCross.Wpf.Views;assembly=MvvmCross.Wpf" 
             x:Class="Training.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Training.WPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="150" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Label Content="User" />
        <TextBox Text="{Binding Username, Mode=TwoWay}" Grid.Column="1" />
        <Label Content="Password" Grid.Row="1"  Margin="0,0,10,0" />
        <PasswordBox x:Name="_passBox" Grid.Row="1" Grid.Column="1" />
        <Button Content="Login" Click="OnLoginClick" Grid.Row="2" />
    </Grid>
</views:MvxWpfView>
